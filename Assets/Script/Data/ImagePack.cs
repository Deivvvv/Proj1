using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//using UnityEngine.UI;


[CreateAssetMenu(fileName = "ImagePack", menuName = "ImagePack/Pack", order = 0)]
public class ImagePack : ScriptableObject
{
    public Sprite[] image;
    public string[] names;
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
